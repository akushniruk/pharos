name: Desktop CI

on:
  push:
    paths:
      - "apps/desktop/**"
      - "apps/client-solid/src/lib/docsPortal.ts"
      - "scripts/check-docs-desktop-web-parity.mjs"
      - "scripts/docs-desktop-web-parity-allowlist.json"
      - ".github/workflows/ci-desktop.yml"
      - ".github/workflows/release-desktop.yml"
      - "scripts/release/**"
      - "CHANGELOG.md"
  pull_request:
    paths:
      - "apps/desktop/**"
      - "apps/client-solid/src/lib/docsPortal.ts"
      - "scripts/check-docs-desktop-web-parity.mjs"
      - "scripts/docs-desktop-web-parity-allowlist.json"
      - ".github/workflows/ci-desktop.yml"
      - ".github/workflows/release-desktop.yml"
      - "scripts/release/**"
      - "CHANGELOG.md"

permissions:
  contents: read

jobs:
  build-and-e2e:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/desktop
    steps:
      - uses: actions/checkout@v4

      - name: Verify desktop version manifests
        working-directory: ${{ github.workspace }}
        run: python3 scripts/release/verify_desktop_versions.py

      - name: Docs desktop ↔ web parity
        working-directory: ${{ github.workspace }}
        run: node scripts/check-docs-desktop-web-parity.mjs

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps

      - name: Build + Playwright tests
        run: npm run test:e2e
